import React from 'react';

const BrushDetails = () => {
    return (
        <div className=''>
           <div class="mockup-code w-full mx-auto">
               
  <pre data-prefix="1"><code>A full stack project</code></pre> 
  <pre data-prefix="2"><code>Two different dashboards for users and admins</code></pre> 
  <pre data-prefix="3" class=""><code>Users can see their ordered products</code></pre>
  <pre data-prefix="4" class=""><code>Users can pay for each items and share reviews</code></pre>
  <pre data-prefix="5" class=""><code>Admin can add products</code></pre>
  <pre data-prefix="6" class=""><code>An admin make another user admin</code></pre>
</div>
        </div>
    );
};

export default BrushDetails;