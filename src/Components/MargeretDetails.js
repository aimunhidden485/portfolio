import React from 'react';

const MargeretDetails = () => {
    return (
        <div>
             <div class="mockup-code w-50 mx-auto">
             <pre data-prefix="1" class=""><code>It's a internet service provider website</code></pre>
               <pre data-prefix="2"><code>Users can login by email and password or google</code></pre> 
               <pre data-prefix="3"><code>If user selects any service they navigated to the checkout form page</code></pre> 
             
             
               
             </div>
        </div>
    );
};

export default MargeretDetails;